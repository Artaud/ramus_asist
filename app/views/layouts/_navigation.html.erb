<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to "KONTAKTNÍK RAMUSISTENTŮ", root_path, class: 'h1 navbar-brand' %>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<%= render 'layouts/navigation_links' %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<% if signed_in? %>
				<li class="navbar-text">
					<% if current_user.email == "asistent@ramus.je" %>
						<%= 'jsi přihlášen/a jako ASISTENT' %>
					<% else %>
					<%= 'jsi přihlášen/a jako ADMINISTRÁTOR: '%>
					<%= Osoba.find_by(id: current_user.osoba_id).jmeno + " " +
					Osoba.find_by(id: current_user.osoba_id).prijmeni %>
					<% end %>
				</li>
				<li class="navbar-text">|</li>
				<li>
					<%= link_to "odhlaš se!", signout_path, method: "delete", :class => "" %>
				</li>

				<% end %>
			</ul>
		</div>
	</div>
</nav>
